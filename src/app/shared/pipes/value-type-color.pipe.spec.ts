import { ValueTypeColorPipe } from './value-type-color.pipe';

describe('ValueTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
